import * as React from 'react'

export interface IconProps {
  onClick: () => {}
}

const Icon = (props: IconProps)  => {

  const classString = {

  }

  return <i {...props} />
}

export default Icon